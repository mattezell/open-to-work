# Roadmap

The model evaluation
notes live in `~/w/opus55/JOURNAL.md`; this repo is the build.

## Decisions

- 2026-09-22: concept approved by Matt: co-op beat 'em up, player 1 in his
  likeness, AI sidekick as player 2, layoff story is fair game.
- 2026-09-22: repo at `~/w/open-to-work` (Matt's call).
- 2026-09-22: sprites via codex imagegen (projects wiki,
  `patterns/llm-generated-game-assets.md`), not the conjure video route.
- 2026-09-22: sidekick is TOKEN, an original robot (design B); Matt is
  design B too ("Bs all around"). Both live in `art/ref/` and are attached
  to every animation prompt.
- 2026-09-22: sim is pure deterministic TS behind a Phaser view, so the
  beatability bot and a future replay validator run the exact game code
  (Momentum's `worker/validate.js` is the reference).
- 2026-09-22: sidekick banter pre-generated by a local model at build time,
  no runtime LLM calls (see DESIGN.md, The sidekick).

## Milestones

- [x] M0 (Tue): repo, design, codex imagegen probe on this box
- [x] M1 (Tue): sim core with tests: belt movement, attack chain, hit-stop,
  knockdown, one enemy type, wave lock; Phaser renders placeholder boxes
  (shipped with real sprites instead of boxes)
- [ ] M2 (Wed): asset pipeline and all actor sheets; Stage 1 playable
- [x] Touch controls (added Tue, not in the original plan): the audience
  for this sees it on a phone
- [x] M3 (Wed): sidekick AI with directives (shipped Tue evening, ce03da4)
- [x] Stage 1 complete (Tue night, ahead of M4, 4b35385): Spam Recruiter, The
  Unpaid Take Home, coffee, Stage 1 wave layout
- [ ] M4 (Thu): Stage 2 (tunnel), Stage 3, The Panel, ending
- [ ] M5 (Thu): synthesized audio, bark bank from qwen3.8-27b
- [ ] M6 (Fri): beatability bot in CI, polish, deploy to Cloudflare
- [ ] M7 (Fri, stretch): leaderboard with replay validation
- [ ] Deep Dive draft for TNG

## Tuning notes

- Stage 1 difficulty is tuned against the CASUAL bot (reaction 15 ticks,
  human mash rate, loose alignment, no special) and pinned by band tests in
  `bot.test.ts`. The bands are a guess at "a first-timer clears the tutorial
  and feels it"; Matt's first real playtest overrides them. Remaining lever
  if it is still too easy: wave size, not wind-up speed (10 ticks is already
  a short telegraph on a phone).
- Enemy RNG barely changes a run (SHARP ends at the same hp on every seed).
  Varied spawn timing or attack choice would help once there are more enemy
  types (M4).
- Stage 1 with the full cast (10 seeds): SHARP clears solo 10/10, CASUAL
  solo 6/10, CASUAL with TOKEN 10/10 (see JOURNAL, M4 Stage 1). A first-timer
  without TOKEN losing 4 in 10 on the boss is intended; check it in Matt's
  playtest.
- The Take Home boss sheets: the walk was regenerated, then recoloured by
  script (olive and peach panels shifted to the manila yellow). It reads
  right at 1x but is a little paler than idle; regenerate once the pipeline
  supports a shared per-character palette (below).
- Barks are placeholder lines in `src/view/barks.ts`; M5 replaces them with
  the generated bank.
- Each sheet gets its own 15-colour snap, so a character's colours drift a
  little between sheets (the ATS walk reads teal against the navy idle).
  Snap all of a character's sheets to one shared palette line.
- HUD text is the browser monospace font, which blurs at 8 px. Replace with
  a bitmap font.
- Stage art is a placeholder skyline drawn in code.
- Touch pad is verified only in emulated Chromium (Playwright, CDP touch
  events). Needs a real iPhone and Android pass: thumb reach of the button
  cluster, whether the landscape buttons hide too much of the right edge,
  and difficulty with a stick instead of keys.

## Open

- Public GitHub repo name and owner
